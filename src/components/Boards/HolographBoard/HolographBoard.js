import React from 'react';
import Modal from '../../Modal/Modal';

function HolographBoard() {
    return ( 
              <Modal title={'Holograph Board'}>
                    I practiced in framer motion. It was landing page, I cut it to practice changing page animation, so it might not be fullscreen
                    <a href="https://github.com/no1cthis/holograph">Github</a>
              </Modal>
     );
}

export default HolographBoard;